//! shrinkray CLI entry point. Subcommands land in the next commit.

fn main() {
    eprintln!("shrinkray CLI v{} — subcommands coming in next commit", env!("CARGO_PKG_VERSION"));
    std::process::exit(2);
}
