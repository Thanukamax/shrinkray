node_modules/
dist/
.vite/
*.log

# Cargo workspace build output (workspace-root target dir, post-v0.4 refactor)
/target/

# Tauri generated bindings
src-tauri/gen/

.vscode/
.idea/
.vs/
*.swp
.DS_Store
Thumbs.db

.env
.env.local

# .NET sidecar build artifacts
sidecar/**/bin/
sidecar/**/obj/
sidecar/**/publish/

# Bundled sidecar binaries (regenerated via scripts/build-sidecar.sh)
src-tauri/binaries/

# Δ-Codec paper bench corpus — regenerated via scripts/fetch-pbr-corpus.sh
# and `cargo run --example gen_synthetic_corpus`. ~140 MB of CC0 / synthetic
# textures; the derived CSVs in paper/results/ are what we actually track.
paper/corpus/
